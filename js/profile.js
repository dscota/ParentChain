var followers = {"dogtor": 67};
var following = {"dogtor": 21};
var followed = [];

function follow_click(profile){
  if (followed.includes(profile) {
    followers[profile] -= 1;
    //change follow button
  }
  else {
    followers[profile] += 1;
    //change follow button
  }
}
